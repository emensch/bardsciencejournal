p Manage users
.user-list
  .user-line(ng-repeat='user in vm.users')
    div {{ user.username }}
    div {{ user.email }}
    button(ng-click='vm.deleteUser(user)') Delete
    button(ng-if='!user.approved', ng-click='vm.approveUser(user)') Approve